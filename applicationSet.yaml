apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: core-applications
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/tyler-harpool/fcsf-environment.git
        revision: HEAD
        directories:
          - path: base/*/overlays/local
  template:
    metadata:
      name: '{{path[1]}}-{{path[2]}}'  # Use more parts of the path to generate unique names
      annotations:
        argocd.argoproj.io/sync-wave: "{{path[1] == 'argocd' && '0' || '1'}}"
    spec:
      project: default
      source:
        repoURL: https://github.com/tyler-harpool/fcsf-environment.git
        targetRevision: HEAD
        path: '{{path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{path[1]}}'  # Ensure the namespace is in lowercase
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
